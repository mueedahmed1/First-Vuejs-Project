# First-Vuejs-Project
Start Learning Vuejs
Basic Project Structure with routing, "Vuex" which is used for state management and user authentication with fake backend 

# Install Dependencies
run command "npm run install"

# Run app
run command "npm run serve"
